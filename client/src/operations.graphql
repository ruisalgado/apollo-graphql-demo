query getAllUsers {
	getUsers {
		id
		name
	}
}

mutation updateUser($id: Int!, $name: String!, $email: String!) {
	updateUser(id: $id, input: { name: $name, email: $email }) {
		id
		name
		email
	}
}